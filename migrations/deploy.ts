// Stretch-path Anchor deployment hook for WhisperPay Agent Vaults.
// BudgetVault is intentionally not wired into the app yet.

module.exports = async function deploy() {
  // No-op on purpose while BudgetVault remains an isolated on-chain skeleton.
};
